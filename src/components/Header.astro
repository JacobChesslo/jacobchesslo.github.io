---
import { NAVIGATIONS } from './constants'
---

<header>
  <h1>Jacob Chesslo</h1>
  <nav>
    {
      NAVIGATIONS.map(item => (
        <a
          title={item.label}
          href={item.href}
        >
          {item.label}
        </a>
      ))
    }
  </nav>
</header>
